{
  "name": "ownership-graph",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm build:shared && pnpm --parallel --filter \"./apps/*\" dev",
    "dev:api": "pnpm --filter @ownership/api dev",
    "dev:web": "pnpm --filter @ownership/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm build:shared && pnpm -r typecheck",
    "format": "prettier --write .",
    "seed": "pnpm build:shared && pnpm --filter @ownership/api seed",
    "build:shared": "pnpm --filter @ownership/shared build"
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  }
}
